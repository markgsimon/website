version: 0.2

phases: 
  install: 
    commands:
      - node --version
      - curl -fsSL https://deb.nodesource.com/setup_16.x | bash -
      - apt-get update
      - apt-get install -y nodejs
      - node --version
      - apt install yarn

  pre_build:
    commands:
      - yarn install

  build:
    commands:
      - yarn build


artifacts:
  files:
    - "**/*"
  discard-paths: no
  base-directory: build